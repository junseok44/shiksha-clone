import { View, Text } from "react-native";
import CenterContainer from "../components/atoms/CenterLayout";

const OrderFavoriteScreen = () => {
  return (
    <CenterContainer>
      <Text>기능 준비중입니다.</Text>
    </CenterContainer>
  );
};

export default OrderFavoriteScreen;
